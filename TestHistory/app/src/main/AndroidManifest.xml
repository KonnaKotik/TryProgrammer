<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.konna.testhistory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Тест по истории"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
        </activity>
        <activity android:name=".activities.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <activity
        android:name=".activities.TestActivity1"
        android:label="Древний мир">
        </activity>
        <activity
            android:name=".activities.TestActivity2"
            android:label="Средние века">
        </activity>
        <activity
            android:name=".activities.TestActivity3"
            android:label="Ну хз какй тут век">
        </activity>
        <activity
            android:name=".activities.TestActivity4"
            android:label="Современность">
        </activity>
    </application>

</manifest>